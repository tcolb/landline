# landline

An agent session runtime with a native mobile app as a first-class client.
`landlined` runs on your box and owns your agent sessions — harness-agnostic,
with real TUI rendering. Your phone dials in.
